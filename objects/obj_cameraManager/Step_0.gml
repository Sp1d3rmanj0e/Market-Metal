/// @description Insert description here
// You can write your code in this editor

// zoom to either 0 or 1
if (zoomed_in) && (zoom < 1) zoom += zoom_speed;
else if (!zoomed_in) && (zoom > 0) zoom -= zoom_speed;

